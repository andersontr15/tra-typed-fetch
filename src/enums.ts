export enum RequestMethod {
  Get = 'GET',
  Destroy = 'DELETE',
  Patch = 'PATCH',
  Put = 'PUT',
  Post = 'POST',
}

export enum RequestParams {
  Body = 'body',
}

export const enum StatusCodes {
  NoContent = 204,
}
